import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.trailerverseone',
  appName: 'Trailer Verse',
  webDir: 'dist'
};

export default config;
